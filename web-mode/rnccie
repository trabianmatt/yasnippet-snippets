# -*- mode: snippet -*-
#key: rnccie
#name: Create React Native Component with Import and Export
# --
import React, {
  Component,
  StyleSheet,
  Text,
  View
} from 'react-native';

let styles;

export default class $1 extends Component {

  render() {
    return (
      ${2:<View style={styles.root}>
        $3
      </View>}
    );
  }
  
}

styles = StyleSheet.create({
  root: {
    flex: 1
  }
});
