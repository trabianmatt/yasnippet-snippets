# -*- mode: snippet -*-
#key: rn
#name: Create React Native Component with Import, Export, and styles
# --
import React, {
  StyleSheet,
  Text,
  View,
} from 'react-native';

export default class $1 extends React.Component {

  render() {
    return(
      ${2:<View>$3</View>}
    );
  }
  
};

const styles = StyleSheet.create({

});